name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    # The authoritative run. Unit tests use the in-memory stores and pass on any
    # platform; the Postgres integration tests only run here, against a real
    # server, so "it worked on my machine" is never the evidence.
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: r0semi_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/r0semi_test?sslmode=disable

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "these files are not gofmt'd:"
            echo "$unformatted"
            exit 1
          fi

      - name: go vet
        run: go vet ./...

      # Runs everything, including the Postgres tests because TEST_DATABASE_URL
      # is set. Without it they skip (that is how local development works).
      - name: go test
        run: go test -race -timeout 300s ./...

      - name: build
        run: go build ./...
