// $lib is imported by path ($lib/api, $lib/components/…), so this barrel is
// intentionally empty. Prefer the specific import: a barrel that re-exports
// everything makes it impossible to see what a page actually depends on.
export {};
